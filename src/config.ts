/**
 * PaymentTracker configuration.
 *
 * Paste your Google OAuth Web Client ID here (ends in .apps.googleusercontent.com),
 * then rebuild + redeploy. The teacher will only ever tap "Sign in with Google".
 *
 * If you leave this empty, the app falls back to letting you paste the Client ID
 * on the login screen (stored on that device only).
 */
export const CLIENT_ID = '294473981083-kodehk3phkmav9i5gm8a950ha9misc2g.apps.googleusercontent.com'
